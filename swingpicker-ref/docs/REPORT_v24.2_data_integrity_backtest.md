# REPORT v24.2 — DATA_INTEGRITY 소급 백테스트 결과 및 demote_official 판단

**데이터:** 추천 이력 77일 (2026-02-26 ~ 06-12) · 거래행 32,463건 · as-of 감사 32,250건
**방법:** 각 추천일의 ohlcv_cache parquet(그날 파이프라인이 본 원본)으로 as-of 감사,
forward 수익률은 combo_optimizer 관례(추천매수가 → D+h snapshot 종가), h=5/10/20.

---

## 1. 헤드라인 — 플래그 종목은 "보유할수록" 썩는다

|  | n | h5 중앙값 | h10 중앙값 | **h20 중앙값** | h20 승률 | h20 p10 |
|---|---|---|---|---|---|---|
| **CLEAN** | 31,812 | -0.30% | -1.41% | **-0.30%** | 49.1% | -25.3% |
| **DI_BAD** (무결성 위반) | 651 | +0.00% | -3.10% | **-14.78%** | **26.4%** | **-34.2%** |

- DI_BAD 651건 = 에피소드 496개. 정체는 대부분 **거래정지/데이터결손 종목군**
  (옵티코어 44일, 우리로 22일, 빛과전자 18일 ... 사유 `V2:비양수가격 + V1:불변식`).
- 진입 단기(h5)에는 차이가 없고, **h10부터 벌어져 h20에서 붕괴**한다 — 4월 포트폴리오를
  갉아먹던 '10일씩 썩는 보유 종목' 프로파일과 정확히 일치.
- h5 평균 +7.3%는 아웃라이어 5건이 끌어올린 허상 (중앙값 0.0%).

## 2. 에이프로젠 — 원인 계측 완료

5/15 as-of 감사: **`V2:비양수가격x14 · V1:OHLC불변식x14 · V3:종가점프1446%`**
직전 20봉 중 **14봉이 0원/음수**였다. 손절 산식이 -66.7%를 뱉은 이유가 데이터
레벨에서 확정. "런타임 디버깅 없이는 규명 불가"였던 문장은 이제 폐기 —
앞으로는 CSV의 `DATA_INTEGRITY_REASON` 한 칸이면 된다.

⚠️ SURGE 그룹(n=15, 전부 에이프로젠 행)의 forward "+95%"는 **진입가 자체가 오염된
행들의 산술 아티팩트**다(실제 체결 결과는 -66.7% 손절). 백테스트 산식마저 깨뜨리는
행이라는 사실 자체가 — 거래 불가 쓰레기 데이터라는 — 제외의 근거다.

## 3. 공식 신호 오염 — 1건, 그러나 치명적

TOP_PICK 66건 중 DI_BAD 교차 **1건**:
**2026-05-12 빛과전자(069540) · ROUTE=ARMED · h5 -21.9% → h20 -43.7%.**
77일 동안 공식 신호가 오염된 건 한 번뿐이지만, 그 한 번이 -44%였다.

참고: `BUY_NOW_ELIGIBLE`은 표본 기간 내내 **전부 0** (게이트가 그만큼 엄격) —
demote_official이 작용하는 경로에 역사적 트래픽이 없었다는 뜻.

## 4. combo_optimizer 자체 판정

DQ ON/OFF를 grid 차원으로 넣고 돌리자(h=3, 256조합), **옵티마이저가 스스로
dq_exclude=1 조합을 최적으로 선택**했다:

| best 조합 (S≥80 T≥80 AI≥70, ATTACK·ARMED) | n | 승률 | EV |
|---|---|---|---|
| DQ OFF | 77 | 74.0% | +12.55 |
| **DQ ON** | 76 | **75.0%** | **+12.85** |

엘리트 필터 레벨에서도 제거 대상 1건이 있었고, 빼는 쪽이 일관되게 낫다.

## 5. 판단 — demote_official

**권고: ON (단, 효과 범위를 정확히 알고 켤 것)**

근거: ① DI_BAD h20 붕괴(중앙값 -14.8%, 승률 26%)는 명백, ② combo도 제외를 선택,
③ BUY_NOW 트래픽이 0이었으므로 **켜도 과거 기준 부작용 0** — 미래 BUY_NOW 경로에
대한 공짜 보험이다.

단, 정직하게: 이번 데이터가 보여준 **실제 노출은 BUY_NOW가 아니라 TOP_PICK/CARRY
경로**다(빛과전자, 그리고 651건의 carry-rot). demote_official은 설계상 TOP_PICK을
건드리지 않으므로(진단 보존 원칙) 빛과전자류는 막지 못한다. 따라서:

1. **지금:** demote_official=True 전환 (1줄 + contract step 13 갱신 — 요청 시 즉시 패치)
2. **다음(v24.3 후보):** DI_BAD를 ELITE_LABEL/TOP_PICK 게이트에 편입할지 — GUARD 통과
   의무와 같은 방식. 근거는 강하지만(group stats) 공식 신호 교차 표본이 1건이라,
   라이브 패널 4~6주 더 쌓고 같은 하네스로 재판정 권장.
3. **운용:** 주 1회 `python scripts/backtest_data_integrity.py --rebuild`로 패널 갱신
   → combo가 자동으로 DQ 차원 포함 탐색.

## 6. 변경 파일 (4개) / 검증

| 파일 | 구분 |
|---|---|
| `scripts/backtest_data_integrity.py` | **신규** — 소급 하네스 (패널·통계·추적) |
| `combo_optimizer.py` | 수정 — DQ merge + dq_exclude 차원 (패널 없으면 기존과 동일) |
| `tests/test_backtest_data_integrity_v242.py` | **신규** — 회귀 10개 |
| `version_info.py` | CHANGELOG 24.2.0 |

검증: tests/ **809 passed** (+10) · contract gate 13단계 ✅ · silent exception 174==baseline ✅ ·
check_deps ✅ · combo 하위호환(패널 부재 시 128조합 동일) 테스트 고정.
파이프라인·추천 산식 변경 없음 — 순수 분석/검증 레이어.
